MOSTVPN config mirror upload folder

Upload the CONTENTS of this folder as the website root.

Required public paths after upload:
/v0/remote_config.json
/v0/manifest.json

This folder contains signed public config artifacts only.
Do not add private keys, .env files, local.properties, or remote_config.input.json.
